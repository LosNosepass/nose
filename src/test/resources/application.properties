# ─── BD en memoria H2 para tests (no necesita MySQL ni phpMyAdmin) ────────────
spring.datasource.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE;MODE=MySQL
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# Hibernate crea y destruye el esquema en cada test
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.show-sql=false

# Consola H2 (opcional, útil para depurar tests)
spring.h2.console.enabled=true

# JWT (mismo valor que producción)
jwt.secret.key=cGFsYWJyYVNlY3JldGFNdXlMYXJnYVBhcmFKV1QyMDI2QWRvcm5vTW92aWxlc0FwaQ==
jwt.expiration.ms=86400000
